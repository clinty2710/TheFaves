// Patch for ws version compatibility

// Patch for ws version compatibility

// Patch for ws version compatibility

// Patch for ws version compatibility

// Patch for ws version compatibility

module.exports = require("./lib").default;
